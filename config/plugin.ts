'use strict';



const validate = {
    enable: true,
    package: 'egg-validate',
};

const sequelize = {
    enable: true,
    package: 'egg-sequelize'
}

const cors = {
    enable: true,
    package: 'egg-cors',
};

export {validate,sequelize ,cors}